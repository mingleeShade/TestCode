#include <iostream>
#include <vector>
#include <list>
#include <map>
#include <unordered_map>
#include <string>
#include "check_big_array.h"
using namespace std;

int main()
{
    size_t nSize = sizeof(gbk2utf8_1) / sizeof(uint32_t);
    cout << "size: " << nSize << endl;
    return 0;
}
